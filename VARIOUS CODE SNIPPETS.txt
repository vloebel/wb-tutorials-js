display the projects
{userState.projects.map(project => <p key={project._id}>{project.title}</p>)}
{userState.projects.filter((p,i)=> i===0).map(project => <p key={project._id}>{project.title}</p>)}
  


  // const [projectState, setProjectState] = useState([]);

//  index to step through projects for editing
  const [projectIndex, setProjectIndex] = useState(0);
  const [maxIndex, setMaxIndex] = useState(0);
  
    if (projectData) {
      //xx do we need the length?
      if ( projectData.projects) { 
        setMaxIndex(projectData.projects.length - 1) 
        setProjectState(projectData.projects);
      }
      else setMaxIndex(-1);